import Button from "@/components/ui/button";

// !Navbar action button
function NavbarActionButton() {
  return (
    <Button href="/categories" data-cy="navbar-action-button">
      Shop Now
    </Button>
  );
}

export default NavbarActionButton;
